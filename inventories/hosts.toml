[hosts.vars]
ansible_user = "{{ username }}"
ansible_port = 50000

[hosts.hosts]
bwh = { ansible_host = "104.243.29.231" }
sg1 = { ansible_host = "167.104.101.86" }

# cu is a CityU workstation, reachable only over the tailnet. It never ran
# init.yaml, so it stays out of the hosts group and keeps the default ssh port.
[clients.vars]
ansible_user = "{{ username }}"

[clients.hosts]
cu = { ansible_host = "100.69.54.3" }

[web.hosts]
sg1 = {}

[proxy.hosts]
bwh = {}
sg1 = {}

[raw]
children = ["bandwagon", "hosthatch"]

[raw.vars]
ansible_ssh_common_args = "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"

[bandwagon.vars]
ansible_user = "root"
ansible_port = 50000

[bandwagon.hosts]
"bwh.raw" = { ansible_host = "104.243.29.231" }

[hosthatch.vars]
ansible_user = "root"
ansible_port = 22

[hosthatch.hosts]
"sg1.raw" = { ansible_host = "167.104.101.86" }
